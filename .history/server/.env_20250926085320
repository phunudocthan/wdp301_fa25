PORT=5000
NODE_ENV=development
JWT_SECRET=your-jwt-secret-key-here-replace-this-in-production

MONGODB_URI=mongodb+srv://admin:123@test.rnfhjxb.mongodb.net/?retryWrites=true&w=majority&appName=test
DB_NAME=lego_ecommerce

# Client URL for CORS
CLIENT_URL=http://localhost:3000


SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587 # 465 nếu dùng ssl, 587 nếu dùng tls nhé cu :)))
SMTP_USER=huybnce170981@fpt.edu.vn #điền tk mail vào đây
SMTP_PASS=ylay iklv auuq kkdy # điền stmp páss vào đây nếu dùng gmail thì vào cài đặt bảo mật của gmail bật xác minh 2 bước và tạo mật khẩu ứng dụng để lấy pass  không config được thì ới anh config cho
